# API 网关开发任务

任务状态：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 里程碑 1：项目初始化

- [ ] 初始化 ThinkPHP 8 项目结构。
- [ ] 配置 PHP 8.0、Composer、MySQL 8.0、Redis 连接。
- [ ] 增加 `.env.example`，覆盖数据库、Redis、网关签名窗口、管理令牌等配置。
- [ ] 配置统一异常响应和基础日志。

验收方式：

- 能启动本地开发服务。
- 能访问 `/health` 并得到 JSON 响应。

## 里程碑 2：数据模型和迁移

- [ ] 创建应用表 `gateway_apps`。
- [ ] 创建路由表 `gateway_routes`。
- [ ] 创建限流策略表 `gateway_rate_limits`。
- [ ] 创建请求日志表 `gateway_request_logs`。
- [ ] 创建管理操作日志表 `gateway_admin_logs`。
- [ ] 编写基础模型和数据访问方法。

验收方式：

- 迁移可在空库执行成功。
- 模型能完成基础 CRUD。

## 里程碑 3：应用和路由管理

- [ ] 实现创建应用接口。
- [ ] 实现应用列表和详情接口。
- [ ] 实现启停应用接口。
- [ ] 实现重置应用密钥接口。
- [ ] 实现创建路由接口。
- [ ] 实现路由列表和启停接口。

验收方式：

- 管理接口返回统一 JSON 结构。
- app_secret 只在创建或重置时返回一次。
- 禁用应用和禁用路由不会参与网关请求。

## 里程碑 4：鉴权与防重放

- [ ] 实现签名原文构造。
- [ ] 实现 HMAC-SHA256 签名校验。
- [ ] 实现时间戳窗口校验。
- [ ] 实现 Redis nonce 去重。
- [ ] 增加鉴权失败日志。

验收方式：

- 正确签名请求可通过。
- 错误签名、过期时间戳、重复 nonce 返回 401。
- 单元测试覆盖签名原文构造和签名校验。

## 里程碑 5：限流

- [ ] 实现应用级固定窗口限流。
- [ ] 实现路由级固定窗口限流。
- [ ] 实现限流策略优先级。
- [ ] 实现 Redis 异常处理。

验收方式：

- 超过阈值返回 429。
- 未超过阈值继续进入路由匹配。
- Redis 不可用时行为符合设计文档。

## 里程碑 6：路由匹配与请求转发

- [ ] 实现方法和路径匹配。
- [ ] 实现精确匹配和前缀匹配。
- [ ] 实现 HTTP 请求转发。
- [ ] 实现上游超时和错误响应。
- [ ] 限制转发 Header 和请求体大小。

验收方式：

- 匹配路由的请求能转发并返回上游响应。
- 未匹配路由返回 404。
- 上游超时返回 504。

## 里程碑 7：日志与健康检查

- [ ] 实现请求日志记录。
- [ ] 实现错误日志记录。
- [ ] 实现管理操作日志。
- [ ] 实现 MySQL 和 Redis 健康检查。

验收方式：

- 每个请求都有 trace_id。
- 日志中不出现完整密钥。
- `/health` 能体现依赖状态。

## 里程碑 8：测试和交付

- [ ] 补充核心 service 单元测试。
- [ ] 补充网关主链路接口测试。
- [ ] 补充异常场景测试。
- [ ] 更新 README 和部署说明。

验收方式：

- 测试通过。
- 文档与实现保持一致。
- 可以按文档在本地启动并验证主链路。

